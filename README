# Cat and The Beanstalk

The app is designed for a local cat sitting business to hold all relevent information about their clients.

### Database Prep

Uses SQLite for the database
Database is called 'CATB'
Database and the table is created on the form page
Information is pulled from the database on the 'Clients' and 'ClientProfile' page
There is one table in the database, 'Clients'
to view the table follow the below link
/Users/hannahwillows/Documents/React Native/CATB/assets/ClientTable.png

- In a new terminal, install the following packages:
  react
  react-native
  express
  react-navigation/native
  react-navigation/native-stack
  expo-sqlite
  expo-image-picker
